import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.portals.onboarding',
  appName: 'Portals Onboarding',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
